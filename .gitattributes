# Default for most files to checkout to platform default
* text=auto

# Force batch and powershell scripts to always use CRLF
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Force bash scripts to always be LF
*.sh text eol=lf
